## Review
- Correct: Strict PGN parser semantics appear preserved. Existing public entry points keep their signatures: `parse_san(board, san) -> Move` delegates to the trace-capable helper and returns only the move (`src/tinychess/engine/pgn.py:171`), while `parse_pgn(text) -> PgnGame` still returns only `.game` (`src/tinychess/engine/pgn.py:177`). The new trace API is additive (`src/tinychess/engine/pgn.py:182`).
- Correct: Sanitizer boundary is still explicit. `parse_ingest_pgn()` and `parse_ingest_pgn_with_trace()` both use `text if strict else sanitize_pgn_text(text)` (`src/tinychess/engine/pgn_stream.py:81`, `src/tinychess/engine/pgn_stream.py:86`), so trace mode does not introduce a separate looser parser.
- Correct: Dense dataset schema/backward compatibility is preserved. PGN ingestion still skips unsupported FEN/setup and unsupported results before writing (`src/tinychess/nn/pgn_dataset.py:148`, `src/tinychess/nn/pgn_dataset.py:157`, `src/tinychess/nn/pgn_dataset.py:160`), and shards still write `positions`, `legal_masks`, `mcts_policies`, and `outcomes` through `SelfPlayDataset`/`save_self_play_dataset()` (`src/tinychess/nn/pgn_dataset.py:295`). Manifest schema/version constants are unchanged (`src/tinychess/nn/pgn_dataset.py:43`, `src/tinychess/nn/pgn_dataset.py:187`).
- Correct: Existing public NN APIs are not removed; NumPy helpers are additive exports (`src/tinychess/nn/__init__.py:22`, `src/tinychess/nn/__init__.py:76`).
- Correct: No generated dataset/checkpoint/data files are present in the current `git status`; changed/untracked files are source/docs/tests/plans/progress only.
- Correct: I ran focused checks only, not the full suite: `uv run pytest tests/test_pgn.py::test_parse_pgn_with_trace_matches_normal_parse_and_replayed_positions tests/test_pgn_stream.py::test_parse_ingest_pgn_with_trace_rejects_same_invalid_records_as_normal_ingest tests/nn/test_pgn_dataset.py::test_ingest_pgn_dataset_trace_path_matches_legacy_replay_arrays tests/nn/test_encode.py::test_numpy_position_encoders_match_mlx_outputs` => 11 passed. I also did a tiny repetition PGN ingest/load smoke; it loaded and recorded `repetition`.
- Blocker: None found for the requested safety/boundary scope.
- Note: Validation gap: committed tests cover normal, mate, castling, en-passant, and promotion replay (`tests/nn/test_pgn_dataset.py:105`), but do not explicitly cover repetition or fifty-move outcomes even though the optimized replay now duplicates those counters (`src/tinychess/nn/pgn_dataset.py:360`, `src/tinychess/nn/pgn_dataset.py:367`, `src/tinychess/nn/pgn_dataset.py:369`) and `_game_record()` derives recorded outcome from that state (`src/tinychess/nn/pgn_dataset.py:469`). Smallest safe fix: add one focused loadable-ingest test for a normal-start threefold repetition PGN, and either a helper-level or normal-start test exercising halfmove clock/fifty-move parity.
- Note: The importer trusts parser-provided legal move tuples for masks and policies after checking board/clocks plus `ply.move in ply.legal_moves` (`src/tinychess/nn/pgn_dataset.py:274`, `src/tinychess/nn/pgn_dataset.py:392`). This is acceptable because ingestion constructs traces via `parse_ingest_pgn_with_trace()` (`src/tinychess/nn/pgn_dataset.py:152`), but keep `_ShardBuilder.add_game()` private/internal. Smallest safe fix if this boundary changes later: add a test-only parity assertion that each trace `legal_moves` equals `legal_moves(ply.board)`; do not recompute in production.
- Note: `scripts/pgn_ingest_benchmark.py` imports private importer helpers (`src/tinychess/nn/pgn_dataset.py` underscored names) at `scripts/pgn_ingest_benchmark.py:26`. This does not affect dataset schema or parser safety, but it makes the benchmark CLI depend on private implementation details. Smallest safe fix: either keep this explicitly script-internal/documented, or move tiny benchmark-only wrappers into the script/public `encode.py` helpers later.
