## Review

- Correct: Strict core PGN parser semantics appear preserved. `parse_san()` and `parse_pgn()` keep their existing public signatures and delegate through the new internal trace-capable path (`src/tinychess/engine/pgn.py:171-184`), while `_parse_pgn()` still rejects unsupported movetext before token handling (`src/tinychess/engine/pgn.py:204-206`).
- Correct: The sanitizer boundary remains explicit and unchanged for ingestion: `parse_ingest_pgn()` and `parse_ingest_pgn_with_trace()` both choose raw strict text vs `sanitize_pgn_text()` in the same way (`src/tinychess/engine/pgn_stream.py:81-88`).
- Correct: Dataset shard compatibility is preserved. Ingestion still skips FEN/SetUp and unsupported `*` results before writing (`src/tinychess/nn/pgn_dataset.py:144-161`), and shard writes still use the existing dense NPZ keys `positions`, `legal_masks`, `mcts_policies`, and `outcomes` (`src/tinychess/nn/self_play.py:334-339`). Manifest action-space and encoder metadata are still emitted (`src/tinychess/nn/pgn_dataset.py:187-194`).
- Correct: Trace reuse is guarded against parser/replay divergence during import by checking trace length and per-ply board/clock state before consuming parser-provided legal moves (`src/tinychess/nn/pgn_dataset.py:267-282`, `src/tinychess/nn/pgn_dataset.py:392-398`).
- Correct: Tests include trace-vs-normal parse coverage (`tests/test_pgn.py:56-80`), strict/sanitized traced ingest parity and rejection parity (`tests/test_pgn_stream.py:42-83`), dense array parity against a legacy replay path (`tests/nn/test_pgn_dataset.py:87-102`), and replay edge cases for mate/castling/en-passant/promotion (`tests/nn/test_pgn_dataset.py:105-168`).
- Correct: Reviewer validation passed:
  - `uv run pytest tests/test_pgn.py tests/test_pgn_stream.py tests/nn/test_pgn_dataset.py tests/test_pgn_ingest_benchmark.py` -> 58 passed.
  - `uv run ruff check src/tinychess/engine/pgn.py src/tinychess/engine/pgn_stream.py src/tinychess/nn/pgn_dataset.py scripts/pgn_ingest_benchmark.py tests/test_pgn.py tests/test_pgn_stream.py tests/nn/test_pgn_dataset.py tests/test_pgn_ingest_benchmark.py` -> passed.
  - `uv run mypy src/tinychess/engine/pgn.py src/tinychess/engine/pgn_stream.py src/tinychess/nn/pgn_dataset.py scripts/pgn_ingest_benchmark.py tests/test_pgn.py tests/test_pgn_stream.py tests/nn/test_pgn_dataset.py tests/test_pgn_ingest_benchmark.py` -> passed.

- Fixed: None. Review-only task; no project/source files were modified.

- Blocker: None found.

- Note [Low, generated-data safety]: Full-write benchmark mode writes to a caller-supplied `--dataset-output-dir` without checking that it is empty (`scripts/pgn_ingest_benchmark.py:240-257`), and the underlying dataset writer creates directories and overwrites known files in-place (`src/tinychess/nn/self_play.py:330-345`). This is not a schema regression, and temp output is safe by default (`scripts/pgn_ingest_benchmark.py:240-243`), but stale extra files from prior runs can remain and output byte/file counts can include them. Smallest safe fix: reject an existing non-empty benchmark output dir unless an explicit `--overwrite`/`--allow-existing-output-dir` flag is passed, or document “use an empty/fresh directory” near `docs/pgn-ingestion.md:83-99`. Test to add/run: full-write benchmark CLI with a pre-populated output dir should either fail clearly or clean/ignore stale files deterministically.

- Note [Low, public API clarity]: New trace carriers and parser entrypoint are public names in `tinychess.engine.pgn` (`src/tinychess/engine/pgn.py:99-115`, `src/tinychess/engine/pgn.py:182-184`) but are not exported from the top-level `tinychess.engine` package (`src/tinychess/engine/__init__.py:17-43`). Existing imports remain backward compatible, so this is not blocking. Smallest safe fix if the trace API is intended public: export `PgnParsedPly`, `PgnGameTrace`, and `parse_pgn_with_trace` from `tinychess.engine.__init__` and add a simple import test; otherwise document it as a module-level/internal ingestion API.

- Note [Low, validation gap]: Added tests cover important trace and replay cases, but dense trace-vs-legacy parity currently uses only the small `PGN_TEXT` accepted-game fixture (`tests/nn/test_pgn_dataset.py:87-102`) and the core trace test uses one normal opening line (`tests/test_pgn.py:56-80`). For additional confidence around boundary risks, add parameterized trace parity cases for checkmate, castling, en-passant, draw/repetition/halfmove-clock progression, and sanitized annotations with clocks/NAGs. Tests to run after that: `uv run pytest tests/test_pgn.py tests/test_pgn_stream.py tests/nn/test_pgn_dataset.py tests/test_pgn_ingest_benchmark.py` plus full `uv run pytest` before merge.
