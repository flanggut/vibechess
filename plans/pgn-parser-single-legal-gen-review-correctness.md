## Review
- Correct: Public parser entry points and trace shape are preserved. `parse_san()` still returns only `Move`, `parse_pgn()` still returns `PgnGame`, and `parse_pgn_with_trace()` still returns `PgnGameTrace` (`src/tinychess/engine/pgn.py:172-185`). Traced plies still capture the pre-move board, halfmove clock, fullmove number, selected move, and the SAN-resolution legal tuple before advancement (`src/tinychess/engine/pgn.py:225-236`).
- Correct: The duplicate `Game.play()` parser replay path has been removed. `_parse_pgn()` now advances through `_PgnParserState.advance_checked(move, legal)` using the legal tuple from `_parse_san_with_legal()` (`src/tinychess/engine/pgn.py:188-202`, `src/tinychess/engine/pgn.py:225-237`) rather than calling `Game.play()` per parsed ply.
- Correct: Parser terminal/outcome ordering mirrors `Game.determine_outcome()` for the non-no-legal cases that can be reached after SAN resolution: fifty-move, repetition, and insufficient material are checked before advancing (`src/tinychess/engine/pgn.py:526-546`). The no-legal check also matches checkmate/stalemate semantics when `advance_checked()` is used directly (`src/tinychess/engine/pgn.py:526-533`).
- Correct: Clock/repetition advancement mirrors `Game.play()`: capture/pawn resets halfmove, black move increments fullmove, and the next board position key increments repetition counts (`src/tinychess/engine/pgn.py:551-564`; reference behavior in `src/tinychess/engine/game.py:100-135`). Added tests cover castling, en-passant, promotion, mate, FEN clocks, repetition, fifty-move, and insufficient-material cases (`tests/test_pgn.py:124-219`).
- Correct: `has_legal_move()` is equivalent to `bool(legal_moves(board))` for the tested representative positions (`src/tinychess/engine/legal_moves.py:57-64`, `tests/test_legal_moves.py:32-47`), and suffix validation still distinguishes no suffix / `+` / `#` (`src/tinychess/engine/pgn.py:500-505`, existing checks at `tests/test_pgn.py:421-428`).
- Correct: Focused validation passed locally: `uv run pytest tests/test_pgn.py tests/test_pgn_stream.py tests/test_legal_moves.py tests/nn/test_pgn_dataset.py tests/test_pgn_ingest_benchmark.py` => 94 passed; `uv run ruff check src/tinychess/engine/pgn.py src/tinychess/engine/legal_moves.py tests/test_pgn.py tests/test_legal_moves.py` => passed; `uv run mypy src/tinychess/engine/pgn.py src/tinychess/engine/legal_moves.py tests/test_pgn.py tests/test_legal_moves.py` => passed.
- Fixed: None; review-only task.
- Blocker: None found.
- Note: The implementation achieves one full `legal_moves()` tuple per parsed ply, but checking moves with `+`/`#` still perform a next-position legal-response search through `has_legal_move()` (`src/tinychess/engine/pgn.py:500-505`; implementation at `src/tinychess/engine/legal_moves.py:57-64`). I verified Fool's Mate parses with 4 `legal_moves()` calls for 4 plies plus 1 `has_legal_move()` call for the mate suffix. This is semantically necessary to distinguish `+` from `#`, but the new tests at `tests/test_pgn.py:65-90` only count `legal_moves()` calls and not `has_legal_move()`. Smallest safe fix if the parent wants the literal “only one legal-generation pass per ply” claim enforced: either (a) reword tests/docs to say “one full legal tuple per ply, with `has_legal_move()` only for check/mate suffix validation,” or (b) add an explicit counter/assertion for `has_legal_move()` suffix calls so benchmark claims cannot be misread.
