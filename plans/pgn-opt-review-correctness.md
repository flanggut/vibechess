## Review
- Correct: SAN trace reuse preserves parser behavior. `parse_san()`/`parse_pgn()` remain stable wrappers, while `_parse_san_with_legal()` returns the selected move plus the exact legal tuple (`src/tinychess/engine/pgn.py:171-201`). Trace plies record the pre-move board, clocks, move, and legal moves before `current.play()` advances (`src/tinychess/engine/pgn.py:217-245`). Ingest strict/sanitized boundaries are identical to the existing entry point (`src/tinychess/engine/pgn_stream.py:81-88`).
- Correct: PGN shard construction uses the traced pre-move board/legal tuple for positions, masks, and one-hot policy, validates trace state/clocks before each sample, and derives outcomes from the parsed PGN sides (`src/tinychess/nn/pgn_dataset.py:267-288`, `src/tinychess/nn/pgn_dataset.py:392-420`). The lightweight replay state mirrors `Game.play()` clock/repetition updates (`src/tinychess/nn/pgn_dataset.py:360-383`).
- Correct: Tensor/mask helpers preserve dense schema and MLX parity. NumPy board encoding writes the same channels/clocks as the MLX encoder (`src/tinychess/nn/encode.py:94-155`), and the NumPy legal-mask helper follows the existing action-index accumulation behavior (`src/tinychess/nn/encode.py:276-290`; importer-local equivalent at `src/tinychess/nn/pgn_dataset.py:401-407`). Tests compare NumPy positions/masks to MLX outputs and trace import arrays to a legacy replay reference (`tests/nn/test_encode.py:80-102`, `tests/nn/test_encode.py:237-255`, `tests/nn/test_pgn_dataset.py:87-102`).
- Correct: Sample counts/outcomes are covered for normal, mate, castling, en-passant, and promotion replay parity (`tests/nn/test_pgn_dataset.py:105-168`). Trace parsing parity/rejection coverage includes sanitized and strict records plus invalid comments, result mismatch, bad check suffix, illegal move, and trailing tokens (`tests/test_pgn.py:56-80`, `tests/test_pgn_stream.py:42-83`).
- Correct: Benchmark paths are valid for this change. Dry-run exercises trace reuse and reports `trace_legal_reuse`/`advance_replay_state` around parser-provided plies (`scripts/pgn_ingest_benchmark.py:156-188`, `scripts/pgn_ingest_benchmark.py:279-309`); full-write calls `ingest_pgn_dataset()` and records shard/sample/output counters (`scripts/pgn_ingest_benchmark.py:247-265`). Benchmark tests assert stable JSON counters and full-write output creation (`tests/test_pgn_ingest_benchmark.py:71-113`).
- Blocker: None found.
- Note: Validation run locally: `uv run pytest tests/test_pgn.py tests/test_pgn_stream.py tests/nn/test_encode.py tests/nn/test_pgn_dataset.py tests/test_pgn_ingest_benchmark.py` (90 passed), `uv run pytest` (277 passed), `uv run ruff check .`, `uv run mypy`, plus dry-run/full-write benchmark CLI smokes with two records (dry-run samples=4, full-write samples=4, shards=2). No fixes required.
