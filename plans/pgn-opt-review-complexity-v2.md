## Review
- Correct: Scope is still narrow for the current optimization series. The diff keeps dense self-play shard keys/schema in `_ShardBuilder.flush()` (`src/tinychess/nn/pgn_dataset.py:290-307`), keeps `parse_san()`/`parse_pgn()` stable while adding a separate trace entry point (`src/tinychess/engine/pgn.py:171-184`), and keeps sanitized ingestion behind `parse_ingest_pgn_with_trace()` (`src/tinychess/engine/pgn_stream.py:86-88`). I did not run tests per the review request; `progress.md` reports focused and full validations already passed.
- Correct: Trace lifetime appears per record/game, not accumulated globally. `ingest_pgn_dataset()` parses one record to a `PgnGameTrace`, immediately sends it to `builder.add_game()`, then moves on (`src/tinychess/nn/pgn_dataset.py:144-168`).
- Fixed: None; review-only task, no source edits made.
- Blocker: None for complexity/maintainability.
- Note: The benchmark script is tightly coupled to importer private internals: it imports `_TrainingReplayState`, `_legal_move_mask_np`, and `_one_hot_policy` from `tinychess.nn.pgn_dataset` (`scripts/pgn_ingest_benchmark.py:26-33`) and mirrors importer trace validation/replay (`scripts/pgn_ingest_benchmark.py:271-307`). This is fragile because benchmark breakage or misleading parity can follow private refactors. Smallest safe fix: move the shared dry-run per-ply helper to a named internal API in `pgn_dataset.py` (or keep benchmark-local helpers using public `encode_board_np`/a public board legal-mask helper) so the script does not import underscore helpers/classes directly.
- Note: Replay state duplicates private `Game.play()` mechanics: `_TrainingReplayState.advance()` repeats capture, clock, fullmove, and repetition updates (`src/tinychess/nn/pgn_dataset.py:360-374`), plus duplicated `_PositionKey`/`_position_key()`/`_is_capture()` from `engine.game` (`src/tinychess/nn/pgn_dataset.py:431-445`; original in `src/tinychess/engine/game.py:100-135` and `src/tinychess/engine/game.py:211-225`). This was expected by the optimization plan, but it is now a maintenance hotspot. Smallest safe fix: extract a tiny engine-level helper for “advance board + clocks + repetition counts” or explicitly document `_TrainingReplayState` as the only duplicate and keep it covered by parity tests.
- Note: `_TrainingReplayState.to_game()` builds a semantically odd `Game` with only the final board in `positions` but all historical `moves` (`src/tinychess/nn/pgn_dataset.py:376-383`). It is currently only used to query `outcome` in `_game_record()` (`src/tinychess/nn/pgn_dataset.py:464-474`), but future callers could misuse it as a normal replayable `Game`. Smallest safe fix: rename it to something narrower like `_outcome_snapshot()`/`to_outcome_game()` and document that it is not a full history game, or compute outcome through an explicit helper without exposing a general `to_game()` method.
- Note: Dry-run phase naming is slightly misleading. `trace_legal_reuse` times a cheap consistency check (`scripts/pgn_ingest_benchmark.py:280-288`), while the actual parser legal generation now lives inside `parse_sanitize` (`scripts/pgn_ingest_benchmark.py:156-163`) and `parse_pgn()` still advances with `Game.play()` (`src/tinychess/engine/pgn.py:224-235`). Smallest safe fix: rename the phase to `validate_trace` or document that `parse_sanitize` includes all SAN legal generation and `trace_legal_reuse` is only validation.
- Note: Full-write benchmark throughput includes artificial record-limiting setup time when `max_records` is set: `_write_limited_records()` is timed as `limit_records` and contributes to `elapsed_seconds` via `total_timed_seconds` (`scripts/pgn_ingest_benchmark.py:62-64`, `scripts/pgn_ingest_benchmark.py:232-238`). That can blur “authoritative importer throughput,” especially on small fixtures. Smallest safe fix: report setup timing separately from measured importer elapsed, or make the JSON/docs explicit that rates include record-copy setup when limiting records.
- Note: There is minor duplication between public `legal_move_mask_from_legal_moves_np(game, legal)` (`src/tinychess/nn/encode.py:276-290`) and ingestion-local `_legal_move_mask_np(board, legal)` (`src/tinychess/nn/pgn_dataset.py:401-407`). Smallest safe fix: add a public/encode-level board-based mask helper and have both call it, avoiding action-index mask logic in two modules.
