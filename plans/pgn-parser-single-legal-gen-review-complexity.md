## Review

- Correct: The parser change is narrowly scoped to private parser replay. `_parse_pgn()` now resolves SAN once with `_parse_san_with_legal()` and passes the same legal tuple into `_PgnParserState.advance_checked()` (`src/tinychess/engine/pgn.py:225-237`), while public `parse_san()`, `parse_pgn()`, and `parse_pgn_with_trace()` signatures remain stable (`src/tinychess/engine/pgn.py:172-185`).
- Correct: The no-history parser state mirrors the relevant `Game.play()`/`determine_outcome()` mechanics closely: terminal checks use the already-computed legal tuple before fifty-move/repetition/insufficient-material checks (`src/tinychess/engine/pgn.py:526-540`, matching `src/tinychess/engine/game.py:138-154`), and move advancement mirrors capture, repetition, halfmove, and fullmove updates (`src/tinychess/engine/pgn.py:551-564`, matching `src/tinychess/engine/game.py:110-134`). This keeps the duplication narrow and private.
- Correct: Performance instrumentation is clear enough for the stated goal. The monkeypatch tests assert one full `legal_moves()` call per parsed ply for normal and checkmate PGNs (`tests/test_pgn.py:65-94`), and the benchmark docs now explain that parser advancement reuses the SAN legal tuple (`docs/pgn-ingestion.md:78-83`).
- Correct: Focused validation passed during this review: `uv run pytest tests/test_pgn.py tests/test_legal_moves.py` => 64 passed; `uv run ruff check src/tinychess/engine/pgn.py src/tinychess/engine/legal_moves.py tests/test_pgn.py tests/test_legal_moves.py` => passed; `uv run mypy src/tinychess/engine/pgn.py src/tinychess/engine/legal_moves.py tests/test_pgn.py tests/test_legal_moves.py` => passed.

- Blocker: None found for complexity/maintainability/performance clarity.

- Note: `_PgnParserState` duplicates a private subset of `Game.play()` and `_position_key()`/`_is_capture()` logic (`src/tinychess/engine/pgn.py:508-581`). This is justified by the performance target and is covered by parity/rejection tests (`tests/test_pgn.py:143-219`), but it is now a maintenance hotspot. Smallest safe follow-up if desired: add a short comment near `_PgnParserState` explicitly naming `Game.play()`/`determine_outcome()` as the source-of-truth logic to update alongside future rule changes.
- Note: `has_legal_move()` avoids constructing a full legal tuple and can early-exit legal filtering, but it still calls eager `pseudo_legal_moves(board)` (`src/tinychess/engine/legal_moves.py:57-64`; `pseudo_legal_moves()` builds and returns a tuple at `src/tinychess/engine/legal_moves.py:22-39`). This is not a blocker for the current single-full-legal-generation goal, but future check/mate-suffix optimization should consider generator-style pseudo move iteration before claiming full early-exit move generation.
- Note: The count test intentionally measures full `legal_moves()` generations, not all legality-related work. Check/mate suffixes still use `has_legal_move()` from `_san_suffix_matches()` (`src/tinychess/engine/pgn.py:500-505`), so the one-call assertion is valid for the stated metric but should not be interpreted as one total pseudo/legal scan per check-suffixed ply.
